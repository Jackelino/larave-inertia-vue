import { createStore } from "vuex";

export default createStore({
    state: {
        contador: 100,
        ead: true,
    },
    getters: {},
    mutations: {},
    actions: {},
    modules: {},
});
